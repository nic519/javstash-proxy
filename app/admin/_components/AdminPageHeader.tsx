'use client';

import type { AdminPageHeaderProps } from './types';

export function AdminPageHeader({ total: _total }: AdminPageHeaderProps) {
  return (
    <div className="animate-fade-in">
    </div>
  );
}
